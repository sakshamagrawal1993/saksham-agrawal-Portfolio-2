# Antigravity planning assignment

Act as the Mind Coach planner. You may write the plan artifact file only; do not implement other application changes.

Read `AGENTS-MIND-COACH.md`, the PRD, the acceptance contract, `docs/MIND_COACH_GAPS.md`, current implementation, migrations, edge functions, existing tests, `.loop-mind-coach/STATE.json`, `planning-contract.json`, and prior loop artifacts.

Map every acceptance ID in the approved planning contract to:

- Current behavior and relevant files
- Existing executable evidence
- Gap or risk
- Test that should prove it
- Smallest proposed implementation change

Prioritize actual broken or unverified user flows. A source file containing a feature is not sufficient evidence that it works.

Write the complete plan to the artifact path supplied by the controller. Include an ordered implementation checklist and focused verification commands.

Never commit, push, open a PR, merge, deploy, publish n8n workflows, or deploy Supabase changes.
